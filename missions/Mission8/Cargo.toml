[package]
name = "mission8"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
criterion = "0.5"

# Benchmarks will be added in REQ-3 (Day 4)
# [[bench]]
# name = "algorithm_benchmarks"
# harness = false
